/* ============================================================
   NINKORO.COM — Supabase 配置
   在 Supabase 控制台 Project Settings → API 复制以下两个值：
   - Project URL
   - anon public key
   留空则站点以纯静态模式运行（不可在线编辑）。
   ============================================================ */
window.NINKORO_SUPABASE = {
  URL: "",
  ANON_KEY: ""
};
